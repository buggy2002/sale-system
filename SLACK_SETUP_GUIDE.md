# 🚀 คู่มือการเชื่อมต่อระบบกับ Slack (Step-by-Step)

คู่มือนี้จะอธิบายขั้นตอนการตั้งค่า Slack App เพื่อใช้งานระบบอนุมัติคำขอเปิดบิล (Approval Workflow) ผ่านทาง Slack

---

## 🛠 สิ่งที่ต้องมี
1. สิทธิ์ระดับ Admin ใน Slack Workspace ของคุณ
2. URL ของระบบที่ออนไลน์อยู่ (เช่น Vercel URL หรือหากทดสอบในเครื่องต้องใช้ `ngrok`)

---

## 1. สร้าง Slack App
1. เข้าไปที่ [Slack API: Your Apps](https://api.slack.com/apps)
2. กดปุ่ม **Create New App**
3. เลือกหัวข้อ **From scratch**
4. กรอกข้อมูล:
   - **App Name**: `Sales Approval Bot` (หรือชื่อที่ต้องการ)
   - **Development Slack Workspace**: เลือกห้องทำงานของบริษัทคุณ
5. กด **Create App**

---

## 2. ตั้งค่าการแจ้งเตือน (Incoming Webhooks)
*ใช้สำหรับส่งข้อความจากระบบเราไปหา Slack เพื่อแจ้งเตือนเมื่อมีคำขอใหม่*

1. ในเมนูซ้ายมือ (หัวข้อ Features) เลือก **Incoming Webhooks**
2. ปรับสวิตช์เป็น **On**
3. กดปุ่ม **Add New Webhook to Workspace** ด้านล่างสุด
4. เลือก Channel ที่ต้องการให้แจ้งเตือน (เช่น `#sales-approvals`) แล้วกด **Allow**
5. คุณจะได้ **Webhook URL** (ตัวอย่าง: `https://hooks.slack.com/services/TXXX/BXXX/XXXX`)
6. **คัดลอกค่านี้ไว้** เพื่อไปใส่ในไฟล์ `.env.local` ภายใต้ชื่อ `SLACK_WEBHOOK_URL`

---

## 3. ตั้งค่าปุ่มกดอนุมัติ (Interactivity)
*ใช้สำหรับให้ปุ่ม "อนุมัติ" หรือ "ปฏิเสธ" ใน Slack ส่งสถานะกลับมาอัปเดตที่โปรแกรมเรา*

1. ในเมนูซ้ายมือ เลือก **Interactivity & Shortcuts**
2. ปรับสวิตช์เป็น **On**
3. ในช่อง **Request URL** ให้ใส่ URL ของระบบคุณ ตามด้วย `/api/slack/interact`
   - *ตัวอย่าง: `https://your-domain.com/api/slack/interact`*
   - *หมายเหตุ: ต้องเป็น HTTPS เท่านั้น*
4. กดปุ่ม **Save Changes** ที่มุมล่างขวา

---

## 4. เก็บค่าความปลอดภัย (Signing Secret)
*ใช้สำหรับให้ระบบเราตรวจสอบว่าข้อมูลที่ส่งมาจาก Slack เป็นของจริงหรือไม่*

1. ในเมนูซ้ายมือ เลือก **Basic Information**
2. เลื่อนลงมาที่หัวข้อ **App Credentials**
3. หาคำว่า **Signing Secret** กด Show แล้ว **คัดลอกค่านี้ไว้**
4. นำไปใส่ในไฟล์ `.env.local` ภายใต้ชื่อ `SLACK_SIGNING_SECRET`

---

## 5. การตั้งค่าในโปรเจกต์ (Environment Variables)

เปิดไฟล์ `.env.local` ในโปรเจกต์ของคุณแล้วอัปเดตค่าดังนี้:

```env
# 1. Webhook ที่ได้จากขั้นตอนที่ 2
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/XXXX/XXXX/XXXX

# 2. Secret ที่ได้จากขั้นตอนที่ 4
SLACK_SIGNING_SECRET=your_signing_secret_here

# 3. URL ของเว็บคุณ (เพื่อให้ปุ่ม "ดูรายละเอียด" ลิ้งก์กลับมาได้ถูกต้อง)
NEXT_PUBLIC_APP_URL=https://your-domain.com
```

---

## 💡 คำแนะนำสำหรับการทดสอบในเครื่อง (Local Testing)
เนื่องจาก Slack ต้องส่งข้อมูลกลับมายังเครื่องคอมพิวเตอร์ของคุณ คุณไม่สามารถใช้ `localhost:3000` ตรงๆ ได้:
1. ติดตั้ง [ngrok](https://ngrok.com/)
2. รันคำสั่ง `ngrok http 3000`
3. คุณจะได้ URL เช่น `https://a1b2-c3d4.ngrok-free.app`
4. นำ URL นี้ไปใช้เป็นฐานในขั้นตอนที่ 3 (Request URL) และใส่ใน `NEXT_PUBLIC_APP_URL`

---

## 🏁 เสร็จสิ้น!
เมื่อตั้งค่าครบแล้ว เมื่อมีการส่งคำขอเปิดบิลใหม่จากหน้า **Sales Portal** ระบบจะส่งข้อความเข้าไปใน Slack ทันที และคุณสามารถกด อนุมัติ/ปฏิเสธ ได้จากมือถือหรือคอมพิวเตอร์ของคุณครับ 🛡️✨
